import { Box } from '@chakra-ui/react'

import styled from '@emotion/styled'

export const LoveSection = styled(Box)`
  padding-left: 3.4em;
  text-indent: -3.4em;
`
export const LoveEmoji = styled.span`
  font-weight: bold;
  margin-right: 2em;
`
